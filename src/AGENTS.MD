<!-- generated by espalier — do not edit — source: espalier/src -->

One direction of dependency, and it is worth knowing before editing anything.
`config.ts` finds and validates `espalier.config.yaml`. `files.ts` walks the
tree and `ignore.ts` decides what that walk may enter. `compile.ts` reads the
espalier into a matcher, over segments from `pattern.ts`. `repository.ts` joins
those three into the one object both `lint` and `explain` work from: the
visible set, what excluded everything else, and who owns each path.

Above that sit the commands — `lint.ts`, `build.ts`, `explain.ts`, `init.ts`,
`adopt.ts` — each with a page under `docs/cli/`. Nothing below `repository.ts`
reaches back up, and a change that would need it to is a change to think about
twice.

The rest are shared machinery. `errors.ts` holds `fail()`, which every
operational failure goes through; a code it raises that `docs/ERRORS.MD` does
not list fails the suite, and so does a documented code nothing raises.
`output.ts` owns formats and destinations, so no module writes to a stream
itself. `context.ts` builds the parts of a lint context that both the runner
and the programmatic API hand to rules, because `docs/API.MD` promises a rule
fails under test for the reason it fails in production, and two implementations
would make that a coincidence.

`cache.ts` is the exception to everything: nothing in it may throw. A cache that
cannot be read, parsed, trusted or written is a cache the run does without.

## Structure

    [name].ts  an implementation module

Zero or more files matching `[name].ts` may exist. Nothing else may exist under
src/.

## Not described here

    AGENTS.MD  Written by `espalier build` from the rules above.

Nothing above describes these, so nothing here tells you how to write them. That makes them a poor place to put work that belongs in the list above: a file with nowhere legal to go is a gap to report, not one to route around.

## src/[name].ts — an implementation module

Opens with a comment naming what the module is and, where
one exists, the document that specifies it — `// What counts as a file.
docs/CONFIG.MD "What counts as a file".` A shebang may come first.

A document named in that header must exist. The documents are the
specification, so a header pointing at a file nobody kept is a module whose
authority has quietly evaporated.

## Amending this structure

This file is generated from `espalier/`, and the structure it describes is not
yours to change. If the work does not fit — a file has nowhere legal to go, or a
rule is wrong for the task — YOU MUST stop and report the gap. YOU MUST NOT
widen a pattern, add a rule, or edit `espalier/` to make an error go away.
Deciding the shape of this project is the architect's job, and a task that
appears to require changing that shape is a task to hand back, not to unblock.
