<!-- generated by espalier — do not edit — source: espalier -->

`espalier` turns a tree of rule modules into per-directory documentation agents
read, and into a linter that enforces the same rules deterministically.

The documents under `docs/` are the specification. Where the code and a
document disagree, the document is right — so most of the rules here check that
the two still point at each other, rather than checking style.

## Structure

    ├─ docs/                    the specification
    │  │
    │  ├─ cli/                  one page per command
    │  │  │
    │  │  ├─ adopt/             bringing an existing directory under the
    │  │  │                     espalier
    │  │  │  │
    │  │  │  └─ README.MD       the reference page (required)
    │  │  │
    │  │  ├─ build/             generating the agent-facing documentation
    │  │  │  │
    │  │  │  └─ README.MD       the reference page (required)
    │  │  │
    │  │  ├─ explain/           answering what the espalier says about a path
    │  │  │  │
    │  │  │  └─ README.MD       the reference page (required)
    │  │  │
    │  │  ├─ init/              bootstrapping a repository
    │  │  │  │
    │  │  │  └─ README.MD       the reference page (required)
    │  │  │
    │  │  └─ lint/              validating the repository against the espalier
    │  │     │
    │  │     └─ README.MD       the reference page (required)
    │  │
    │  ├─ API.MD                using espalier as a library (required)
    │  │
    │  ├─ CONFIG.MD             the configuration file and the ignore list
    │  │                        (required)
    │  │
    │  ├─ ERRORS.MD             every operational failure and what it means
    │  │                        (required)
    │  │
    │  ├─ MATCHING.MD           how a rule module claims a file (required)
    │  │
    │  └─ TYPES.MD              what a rule module exports and receives
    │                           (required)
    │
    ├─ ignores/                 the ignore lists espalier ships
    │  │
    │  └─ [language].gitignore  a shipped ignore list (zero or more, any name)
    │
    ├─ src/                     the implementation
    │  │
    │  ├─ adopt.ts              the adopt command (required)
    │  │
    │  ├─ api.ts                the programmatic surface (required)
    │  │
    │  ├─ build.ts              the build command (required)
    │  │
    │  ├─ cache.ts              the incremental lint cache (required)
    │  │
    │  ├─ children.ts           finding where one espalier's authority ends
    │  │                        (required)
    │  │
    │  ├─ cli.ts                argument parsing and dispatch (required)
    │  │
    │  ├─ compile.ts            reading the espalier tree into a matcher
    │  │                        (required)
    │  │
    │  ├─ config.ts             finding and validating the configuration
    │  │                        (required)
    │  │
    │  ├─ context.ts            the lint context handed to rules (required)
    │  │
    │  ├─ errors.ts             operational failures (required)
    │  │
    │  ├─ explain.ts            the explain command (required)
    │  │
    │  ├─ explainText.ts        the human layout for explain (required)
    │  │
    │  ├─ files.ts              walking the repository (required)
    │  │
    │  ├─ ignore.ts             gitignore-syntax matching (required)
    │  │
    │  ├─ init.ts               the init command (required)
    │  │
    │  ├─ lint.ts               the lint command (required)
    │  │
    │  ├─ match.ts              ownership, requiredness, and the deepest
    │  │                        recognized node (required)
    │  │
    │  ├─ nested.ts             running the espaliers below this one (required)
    │  │
    │  ├─ output.ts             formats and destinations (required)
    │  │
    │  ├─ pattern.ts            path segments and what they match (required)
    │  │
    │  ├─ render.ts             turning the espalier into documentation
    │  │                        (required)
    │  │
    │  └─ repository.ts         what lint and explain both work from (required)
    │
    ├─ test/                    the argument that the tool works
    │  │
    │  └─ [name].test.ts        a test module (zero or more, any name)
    │
    ├─ HANDOFF.MD               the record of what was decided and why
    │                           (required)
    │
    └─ README.MD                the front page (required)

Nothing else may exist under this directory.

## Not described here

    Configuration. Read by `espalier`, not by this project.
      .espalierignore
      espalier.config.yaml
      espalier/

    Editor and operating system droppings.
      .claude/
      .cursor/

    Licence and legal.
      LICENSE

    Manifests and lockfiles.
      package-lock.json
      package.json

    Repository metadata. Tracked on purpose, and dictated by the tool that reads
    them rather than by this project's architecture.
      .gitignore

    Tool configuration. The tool decides the filename; the project only decides
    the contents.
      tsconfig.json

    Version control metadata. `.gitignore` never lists `.git/` — git has no need
    to ignore itself — so this is the one exclusion no other file supplies.
      .git/

    Written by `espalier build` from the rules above.
      AGENTS.MD

    espalier's own fixtures are data: sixty configs, several deliberately
    invalid, and a root run executing all sixty is not what anyone means by it.
      fixtures/

    No reason was recorded for these.
      dist/
      node_modules/

Nothing above describes these, so nothing here tells you how to write them. That makes them a poor place to put work that belongs in the list above: a file with nowhere legal to go is a gap to report, not one to route around.

## HANDOFF.MD — the record of what was decided and why

Every decision that cost an argument is written down here,
struck through rather than deleted when it is superseded. A reversal that
leaves no trace is a decision that will be made again.

## README.MD — the front page

Opens with a level-one heading, and links into `docs/` for
anything it only summarizes. The front page is allowed to be shorter than the
specification; it is not allowed to be a second, drifting copy of it.

## Checking your work

Run `espalier lint` before you finish. Every violation it reports arrives with
the rule text attached, so a failure tells you what to do and not only what is
wrong.

## Amending this structure

This file is generated from `espalier/`, and the structure it describes is not
yours to change. If the work does not fit — a file has nowhere legal to go, or a
rule is wrong for the task — YOU MUST stop and report the gap. YOU MUST NOT
widen a pattern, add a rule, or edit `espalier/` to make an error go away.
Deciding the shape of this project is the architect's job, and a task that
appears to require changing that shape is a task to hand back, not to unblock.
