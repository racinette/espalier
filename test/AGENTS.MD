<!-- generated by espalier — do not edit — source: espalier/test -->

# espalier — test/

Two kinds of test, and the division is not stylistic.

`fixtures.test.ts` runs the conformance suite: every directory under
`fixtures/` is a miniature repository with an `expected.json`, and a
`FIXTURE.MD` naming the sentence of which document it pins. That is where
behaviour belongs, because a fixture is readable by someone who has never seen
the implementation.

Everything else here is for what a fixture cannot see. A cache that was not
written, a directory that was never entered, a run that refused before writing
anything: the observable output is identical either way, so the assertion has
to reach past it. If a case can be a fixture, it should be.

A test that passes is not the same as a test that guards. Break the code and
watch the new test fail before believing it — this suite has shipped cases that
passed with the fix reverted.

## Project layout

    test/
    └─ [name].test.ts  a test module (zero or more, any name)

## test/[name].test.ts — a test module

Opens with a comment saying what this file covers and why
it exists as a unit test rather than as a fixture. The suite is the argument
that the tool works; a file that cannot say what it is guarding is one nobody
will dare delete.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Modify [`ESPALIER.MD`](../espalier/test/ESPALIER.MD) to change this document's persistent guidance. |

Every path under test/ must fit the project layout or a documented
repository-specific purpose.
