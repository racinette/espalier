# unexpected_path: the deepest recognized node

Three degradation cases for the metadata payload:

  clients/acme/random.ts   walk reaches a dynamic directory — captures reported
  components/Modal.tsx     walk reaches a static directory — children reported
  backend/orders/x.ts      walk recognizes nothing — no suggestion at all

The declared list is never a ranked guess at where the file belongs.

Pins: MATCHING.MD "The deepest recognized node".
