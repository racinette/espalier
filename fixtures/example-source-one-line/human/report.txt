
components/buttons/SubmitButton.tsx
  info  seen   the rule ran

    Rule (a button component):
      Default-export a single component named after the file.

    Reference implementation:
      export default function Submit() { … }

1 info
