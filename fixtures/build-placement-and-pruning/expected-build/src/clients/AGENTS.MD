<!-- generated by espalier — do not edit — source: espalier/src/clients -->

# demo-app — src/clients/

Every upstream provider gets its own directory. Nothing outside
`src/clients/` performs HTTP requests directly.

## Project layout

    src/clients/
    └─ [provider]/          one directory per upstream provider (zero or more,
                            any name)
       │
       ├─ handlers/
       │  │
       │  └─ webhook.ts     an inbound webhook handler (required)
       │
       ├─ client.ts         an integration with an external API (required)
       │
       └─ requestModels.ts  request payload types (required)

## src/clients/[provider]/ — one directory per upstream provider

Name the directory after the provider as they spell it: `stripe`, not
`Stripe`.

## src/clients/[provider]/handlers/webhook.ts — an inbound webhook handler

Verify the signature before parsing the body.

## src/clients/[provider]/client.ts — an integration with an external API

Export a single `create<Provider>Client(config)` factory.

Example: src/clients/stripe/client.ts

## src/clients/[provider]/requestModels.ts — request payload types

Types only, no runtime code.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Modify [`ESPALIER.MD`](../../espalier/src/clients/ESPALIER.MD) to change this document's persistent guidance. |

Every path under src/clients/ must fit the project layout or a documented
repository-specific purpose.
