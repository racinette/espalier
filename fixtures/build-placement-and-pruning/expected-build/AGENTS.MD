<!-- generated by espalier — do not edit — source: espalier -->

# demo-app

A demonstration repository. Everything here exists to show how the espalier
renders.

## Project layout

    ./
    ├─ components/
    │  └─ buttons/
    ├─ src/
    │  │
    │  └─ clients/  outbound API integrations
    │
    └─ main.ts      the application entry point (required)

## main.ts — the application entry point

Wire up the application and start it. No business logic here.

## Constraints

### TypeScript files

These rules apply to all TypeScript files in this project.

#### no-as-any — applies to TypeScript files throughout the project

Never write `as any`. Narrow the type instead.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `.espalierignore`<br>`espalier`<br>`espalier.config.yaml` | Sources used to generate and check this repository guidance. |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Modify [`ESPALIER.MD`](espalier/ESPALIER.MD) to change this document's persistent guidance. |

Every path in this directory must fit the project layout or a documented
repository-specific purpose.

## Checking your work

Run `espalier lint` before you finish. Every violation it reports arrives with
the rule text attached, so a failure tells you what to do and not only what is
wrong.

## Amending this structure

This file is generated from `espalier/`, and the structure it describes is not
yours to change. If the work does not fit — a file has nowhere legal to go, or a
rule is wrong for the task — YOU MUST stop and report the gap. YOU MUST NOT
widen a pattern, add a rule, or edit `espalier/` to make an error go away.
Deciding the shape of this project is the architect's job, and a task that
appears to require changing that shape is a task to hand back, not to unblock.
