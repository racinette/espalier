# ownership: static beats dynamic

A static leaf and a dynamic leaf in the same directory both match
`requestModels.ts`. The static rule owns it; the dynamic rule does not run.

Pins: MATCHING.MD "Ownership", and captures on the winning rule.
