<!-- generated by espalier — do not edit — source: espalier/src -->

## Structure

    src/
    ├─ important.log  the retained log example (required)
    │
    └─ main.ts        the source entry point (required)

Nothing else may exist under src/.

## src/important.log — the retained log example

Keep the stable example used by tests.

## src/main.ts — the source entry point

Implement the application entry point.

## Not described here

### Exclusion rules

Rules appear in evaluation order. A leading `!` retains a path that an earlier rule excluded.

| Rules | Why |
| --- | --- |
| `/src/generated/` | Source compiler output. |
| `!important.log` | Keep the source log used as a checked-in example. |
| `scratch/` | Source-local scratch data. |

### Tool-owned entries

| Entries | Why |
| --- | --- |
| `.espalierignore` | Configuration. Read by `espalier`, not by this project. |
| `AGENTS.MD` | Generated by `espalier build`; edits here will be overwritten. Modify [`ESPALIER.MD`](../espalier/src/ESPALIER.MD) to change this document's persistent guidance. |

Nothing above describes these as project structure. They are not alternative destinations for work that does not fit the structure above.

## Amending this structure

This file is generated from `espalier/`, and the structure it describes is not
yours to change. If the work does not fit — a file has nowhere legal to go, or a
rule is wrong for the task — YOU MUST stop and report the gap. YOU MUST NOT
widen a pattern, add a rule, or edit `espalier/` to make an error go away.
Deciding the shape of this project is the architect's job, and a task that
appears to require changing that shape is a task to hand back, not to unblock.
