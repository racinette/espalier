<!-- generated by espalier — do not edit — source: espalier/backend -->

## document-heading

Each file with exactly `.MD` as its extension under `backend/` must follow this
rule.

Open with a heading.

## no-fetch

Each file with exactly `.ts` as its extension under `backend/` must follow this
rule.

Call the shared client, never fetch directly.

## opaque-format

Each file with exactly `.WEIRD` as its extension under `backend/` must follow
this rule.

Keep the opaque format readable.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `backend/**` | — |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Create [`ESPALIER.MD`](../espalier/backend/ESPALIER.MD) to add persistent guidance to this document. |
