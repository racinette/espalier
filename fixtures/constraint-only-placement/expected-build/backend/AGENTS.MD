<!-- generated by espalier — do not edit — source: espalier/backend -->

## Per-file constraints

### Markdown files under `backend/`

These rules are evaluated once per selected file. They apply to every owned Markdown file under `backend/`.

#### document-heading

Open with a heading.

### TypeScript files under `backend/`

These rules are evaluated once per selected file. They apply to every owned TypeScript file under `backend/`.

#### no-fetch

Call the shared client, never fetch directly.

### `.WEIRD` files under `backend/`

These rules are evaluated once per selected file. They apply to every owned `.WEIRD` file under `backend/`.

#### opaque-format

Keep the opaque format readable.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `backend/**` | — |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Create [`ESPALIER.MD`](../espalier/backend/ESPALIER.MD) to add persistent guidance to this document. |
