# Adopt force migrates build-owned documentation

The configured output already contains hand-written guidance, and the durable
`ESPALIER.MD` already contains prose of its own. Forced adoption preserves both,
marks the migrated block by source path, and still writes the inferred rule.
