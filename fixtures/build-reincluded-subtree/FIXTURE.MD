# Build: an explicitly re-included subtree

A broad Espalier exclusion hides generated trees across packages. The API
client subtree is deliberately governed, so both its directory and its
contents are re-included. The generated guidance must show the client in the
structure while retaining the broad exclusion for the materialized sibling
subtrees.
