# Different back-references may collide

Pins MATCHING.MD "Ambiguity is rejected": region and provider can both be eu,
so their resolved siblings must be rejected even without repository files.
Sharing a suffix does not make different captures the same binding.
