<!-- generated by espalier — do not edit — source: espalier/clients -->

## Project layout

    clients/
    └─ [provider]/       (zero or more, any name)
       │
       ├─ {provider}.ts  the provider's entry point (required, {provider}
       │                 repeats what [provider] matched)
       │
       └─ [helper].ts    a helper (zero or more, any name)

## clients/[provider]/{provider}.ts — the provider's entry point

Export a single factory named after the file.

## clients/[provider]/[helper].ts — a helper

Anything the entry point needs and nobody else does.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Create [`ESPALIER.MD`](../espalier/clients/ESPALIER.MD) to add persistent guidance to this document. |

Every path under clients/ must fit the project layout or a documented
repository-specific purpose.
