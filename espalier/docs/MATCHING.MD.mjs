export const description = "how a rule module claims a file";

export const rule = `Covers the espalier tree: how a path inside it becomes a pattern, which pattern
owns a file when several could, and what must exist. What a module exports is
TYPES.MD; what a command does with the answer is its page under \`docs/cli/\`.`;

export async function lint() {}
