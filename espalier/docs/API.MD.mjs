export const description = "using espalier as a library";

export const rule = `Covers the package: what it exports, what those functions return, and what a
caller must build to test a rule. A rule module's own contract is TYPES.MD;
this is the other side of that boundary.`;

export async function lint() {}
