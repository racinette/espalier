export const description = "the configuration file and the ignore list";

export const rule = `Covers \`espalier.config.yaml\` and \`.espalierignore\`: every key, how the root is
discovered, where a child espalier's authority begins, and what is excluded
from the walk. How a declared path claims a file is MATCHING.MD.`;

export async function lint() {}
