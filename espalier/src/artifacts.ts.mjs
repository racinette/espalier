export const description = "the target-neutral generated artifact model";
export const rule = `Define generated outputs independently of filesystem application so multiple targets can share one atomic build transaction.`;

export async function lint() {}
