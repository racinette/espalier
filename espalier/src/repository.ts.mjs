export const description = "what lint and explain both work from";

export const rule = `Joins the config, the walk and the compiled espalier into one object: the
visible set, what excluded everything else, and who owns each path. Nothing
below it reaches back up, and a change that would need it to is a change to
think about twice.`;

export async function lint() {}
