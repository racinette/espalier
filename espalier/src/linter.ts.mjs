export const description = "contextual typing helpers for rule linters";

export const rule = `Exports identity helpers and the context types they apply.
Plain lint functions remain valid; typing helpers must not introduce a second
runtime protocol.`;

export async function lint() {}
