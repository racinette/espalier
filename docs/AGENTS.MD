<!-- generated by espalier — do not edit — source: espalier/docs -->

These are not notes about the implementation. They are the specification, and
where the code and one of them disagree the document is right — so a behaviour
change is a change here first, and a document that is ambiguous or
self-contradictory is something to report rather than to interpret.

Each covers one subject and says why, not only what. A decision recorded
without its reasoning is one that gets reversed by the next person who finds it
inconvenient; `HANDOFF.MD` at the root exists for the same reason and keeps
superseded decisions struck through rather than deleted.

## Structure

    cli/           one page per command
      [command]/
        README.MD  the page for one command
    [doc].MD       a specification document

Zero or more `cli/[command]/` directories may exist. For each one that does,
README.MD is required. Zero or more files matching `[doc].MD` may exist. Nothing
else may exist under docs/.

## docs/[doc].MD — a specification document

Named in upper case, one document per subject, opening with
a level-one heading. These are the specification: where the code and one of
these disagree, the document is right, and a code change that contradicts one
is a change to the document first.

## Amending this structure

This file is generated from `espalier/`, and the structure it describes is not
yours to change. If the work does not fit — a file has nowhere legal to go, or a
rule is wrong for the task — YOU MUST stop and report the gap. YOU MUST NOT
widen a pattern, add a rule, or edit `espalier/` to make an error go away.
Deciding the shape of this project is the architect's job, and a task that
appears to require changing that shape is a task to hand back, not to unblock.
