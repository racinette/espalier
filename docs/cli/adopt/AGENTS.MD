<!-- generated by espalier — do not edit — source: espalier/docs/cli/adopt -->

# espalier — `docs/cli/adopt/`

## Project layout

    docs/cli/adopt/
    └─ README.MD  the reference page (required)

## `README.MD` — the reference page

Opens with the invocation as its level-one heading, then a
`## Usage` block listing every flag `espalier adopt` accepts. A reader
looking up a command should not have to read the parser.

The page and the implementation are two halves of one claim. `src/adopt.ts`
is what this page describes, and a page for a command nobody wrote is this tree
lying about what the tool does.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Modify [`ESPALIER.MD`](../../../espalier/docs/cli/adopt/ESPALIER.MD) to change this document's persistent guidance. |

Every path under docs/cli/adopt/ must fit the project layout or a documented
repository-specific purpose.
