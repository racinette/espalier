<!-- generated by espalier — do not edit — source: espalier/docs/cli/lint -->

# espalier — `docs/cli/lint/`

## Project layout

    docs/cli/lint/
    └─ README.MD  the reference page (required)

## `README.MD` — the reference page

Opens with the invocation as its level-one heading, then a
`## Usage` block listing every flag `espalier lint` accepts. A reader
looking up a command should not have to read the parser.

The page and the implementation are two halves of one claim. `src/lint.ts`
is what this page describes, and a page for a command nobody wrote is this tree
lying about what the tool does.

## Other repository paths

These paths are part of the repository but serve specific purposes outside the project layout. Use them only for the purpose stated here.

| Paths | Purpose |
| --- | --- |
| `AGENTS.MD` | Generated repository guidance; edits here will be overwritten. Modify [`ESPALIER.MD`](../../../espalier/docs/cli/lint/ESPALIER.MD) to change this document's persistent guidance. |

Every path under docs/cli/lint/ must fit the project layout or a documented
repository-specific purpose.
