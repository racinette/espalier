<!-- generated by espalier — do not edit — source: espalier/docs/cli/init -->

# espalier — docs/cli/init/

## Structure

    docs/cli/init/
    └─ README.MD  the reference page (required)

Nothing else may exist under docs/cli/init/.

## docs/cli/init/README.MD — the reference page

Opens with the invocation as its level-one heading, then a
`## Usage` block listing every flag `espalier init` accepts. A reader
looking up a command should not have to read the parser.

The page and the implementation are two halves of one claim. `src/init.ts`
is what this page describes, and a page for a command nobody wrote is this tree
lying about what the tool does.

## Not described here

### Tool-owned entries

| Entries | Why |
| --- | --- |
| `AGENTS.MD` | Generated by `espalier build`; edits here will be overwritten. Modify [`ESPALIER.MD`](../../../espalier/docs/cli/init/ESPALIER.MD) to change this document's persistent guidance. |

Nothing above describes these as project structure. They are not alternative destinations for work that does not fit the structure above.

## Amending this structure

This file is generated from `espalier/`, and the structure it describes is not
yours to change. If the work does not fit — a file has nowhere legal to go, or a
rule is wrong for the task — YOU MUST stop and report the gap. YOU MUST NOT
widen a pattern, add a rule, or edit `espalier/` to make an error go away.
Deciding the shape of this project is the architect's job, and a task that
appears to require changing that shape is a task to hand back, not to unblock.
