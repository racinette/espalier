LEGACY_README.MD
node_modules/
dist/

# espalier writes one per espalier, including inside fixtures.
.cache/
# except the one a fixture ships on purpose.
!fixtures/lint-cache-not-trusted/espalier/.cache/
